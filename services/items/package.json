{
  "name": "sparkboard-items-service",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda handler for items management",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.723.0",
    "@aws-sdk/lib-dynamodb": "^3.723.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
